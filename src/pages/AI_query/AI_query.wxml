<!-- pages/AI_query/AI_query.wxml -->
<view style="margin-top: 50rpx; width: 80%; margin : 0 auto">
	<view class="question-container">
    <!-- <van-grid clickable column-num="1">
        <navigator class="question" url="/src/pages/AI_query/answers/1.html">1. 什么是家庭关联账户</navigator>
        <van-grid-item text="2. 如何申请家庭关联账户">
        </van-grid-item>
    </van-grid> -->
    <view class="question_text" wx:for="{{question_list}}" wx:for-item="question" wx:key="id" hover-class="question-hovered" bindtap="navigate_page" data-operation="{{question.id}}">
        {{question.id}}.{{question.text}}
    </view>
</view>
</view>

<!-- <form class="page-form" bindsubmit="formSubmit" bindreset="formReset">
    <view class="group">
        <input class="question" type="text" bindfocus="inputFocus" name="question" auto-focus="true" placeholder="请简洁描述您的问题" value="{{entry}}" />
        <button class="cancel" form-type="reset">×</button>
        <button class="search" type="primary" form-type="submit" animation="{{animationData}}">
            问
        </button>
    </view>
</form>
<scroll-view class="page-result" scroll-y="true" scroll-into-view="{{toView}}">
    <block wx:for="{{user}}" wx:for-item="qa">
        <view class="row" id="toView{{qa.id}}">
            <text class="user-question">{{qa.question}}</text>
            <view class="sys-anwser">
                {{qa.anwser}}
                <navigator wx:if="{{qa.url}}" url="qa.url">{{qa.url}}</navigator>
            </view>
        </view>
    </block>
</scroll-view>
<loading hidden="{{hidden}}">加载中...</loading>
<toast hidden="{{toastHidden}}" duration="2000" bindchange="toastChange">{{errMsg}}</toast> -->