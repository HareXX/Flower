<van-cell-group>
 	<van-field
		label="手机号"
		value="{{ value }}"
		placeholder="请输入手机号"
		border="{{ false }}"
		bind:change="onChange"
		shape="round"
		center
		custom-style="width : 80%; margin : 0 auto"
  	/>
</van-cell-group>